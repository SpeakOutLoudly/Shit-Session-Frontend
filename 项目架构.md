# 项目架构文档

## 概述

智能会议同传与纪要系统前端，基于 Vue 3 + Pinia + Vue Router + Vite。对接后端 REST API + SSE 实时字幕流，会议音频由边缘设备采集识别，前端只负责展示和交互。

---

## 根目录文件

### `package.json`

项目配置和依赖管理。定义 `scripts`——`dev`（开发服务器）、`build`（生产构建）、`preview`（预览构建产物）。依赖：`vue`、`vue-router`、`pinia`。

### `vite.config.js`

Vite 构建配置。启用 Vue 插件，开发服务器端口 3000，配置 `/api` 代理到后端 `http://127.0.0.1:8080`。

### `index.html`

Vite 入口 HTML。挂载点 `<div id="app">`，引入 `/src/main.js`。

### `README.md`

项目说明文档——快速开始、技术栈、项目结构、系统架构、API composable 一览、用户流程、开发历程（v1-v6，含完整 Bug 修复记录）。

---

## `src/` 源码目录

### `src/main.js` — 应用入口

创建 Vue 应用实例，注册 Pinia（状态管理）、Vue Router（路由），挂载到 `#app`。

---

### `src/App.vue` — 根组件

**主布局容器。** 根据当前路由决定显示哪个界面：

- **登录界面**：`route.name === 'Login'` 时全屏渲染 Login 页面，无侧边栏
- **系统主界面**：侧边栏（可折叠导航 + 系统状态） + 顶部栏（页面标题 + 用户信息 + 退出按钮） + `<router-view>` 内容区

**职责：**
- 导航菜单定义（5 个页面：会议室/工作台/会议摘要/待办任务/导出同步）
- 侧边栏 badge（待办任务数量，从 `store.tasks` 实时计算）
- 退出登录（清 localStorage 跳转登录页）

---

### `src/router/index.js` — 路由配置

| 路径 | 名称 | 说明 |
|------|------|------|
| `/` | - | 重定向到 `/transcription` |
| `/login` | Login | 登录页（由 App.vue 直接渲染） |
| `/dashboard` | Dashboard | 工作台 |
| `/transcription` | Transcription | 会议室（核心页） |
| `/summary` | Summary | 会议摘要 |
| `/tasks` | Tasks | 待办任务 |
| `/export` | Export | 导出与同步 |

---

### `src/assets/styles.css` — 全局样式

CSS 变量系统（科技蓝主题色 `--primary: #4F6EF7`），全局 reset，组件级通用样式类（按钮 `.btn`、卡片 `.card`、标签 `.tag`、表单 `.form-input`、弹窗 `.modal`、进度条 `.progress-bar`、统计卡片 `.stat-card`、表格、动画过渡等）。

---

## `src/stores/meetingStore.js` — Pinia 状态仓库

**职责：** 轻量缓存层，不存储 mock 数据，数据由 composable 从 API 获取后写入。

| 状态 | 类型 | 说明 |
|------|------|------|
| `username` | `ref('')` | 当前用户名（localStorage 持久化） |
| `currentMeeting` | `ref(null)` | 当前选中的会议对象（MeetingVO） |
| `meetingStatus` | computed | 从 `currentMeeting` 推导的会议状态枚举 |
| `meetingMinutes` | `ref(null)` | 当前会议纪要对象（MeetingMinutesVO） |
| `tasks` | `ref([])` | 当前会议的任务列表（MeetingTaskVO[]） |
| `meetingList` | `ref([])` | 工作台会议列表 |
| `meetingListTotal` | `ref(0)` | 会议总数 |
| `stats` | computed | 从 `meetingList` 统计的会议数量/状态分布 |

**方法：** `setUsername()` 设置用户名并持久化。

---

## `src/composables/` — 可组合逻辑层

### `useApi.js` — API 基础层

**核心请求封装：** 基于 `fetch`，统一处理：

- base URL `/api/v1` 前缀
- 请求/响应 JSON 序列化
- 通用响应结构 `{ code, message, data }` —— 只返回 `data`，非 200 抛异常
- URL 查询参数构建（过滤空值）
- **3 秒超时**（`AbortController`），无后端时快速降级

**导出方法：** `get()` `post()` `put()` `patch()` `del()` `getDownloadUrl()`

---

### `useMeeting.js` — 会议管理

封装所有会议 REST 接口，调用后将结果写入 `store.currentMeeting` / `store.meetingList`。

| 方法 | 后端接口 | 用途 |
|------|---------|------|
| `createMeeting(data)` | `POST /meetings` | 创建会议 |
| `getMeeting(id)` | `GET /meetings/{id}` | 查询会议详情 |
| `listMeetings(params)` | `GET /meetings` | 分页查询会议列表 |
| `updateMeeting(id, data)` | `PUT /meetings/{id}` | 更新会议信息 |
| `startMeeting(id)` | `POST /meetings/{id}/start` | 开始会议（通知边缘设备录制） |
| `finishMeeting(id)` | `POST /meetings/{id}/finish` | 结束会议 |
| `cancelMeeting(id)` | `POST /meetings/{id}/cancel` | 取消会议 |
| `deleteMeeting(id)` | `DELETE /meetings/{id}` | 删除会议 |
| `clearCurrentMeeting()` | - | 清空 store 中的当前会议 |

---

### `useSubtitleSSE.js` — SSE 实时字幕流

基于 `EventSource` 连接后端 SSE 端点 `/api/v1/meetings/{id}/subtitles/stream`。

**状态：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `isConnected` | `ref(false)` | SSE 连接状态 |
| `interimText` | `ref('')` | 临时识别结果（正在说的内容） |
| `finalSegments` | `ref([])` | 最终字幕片段列表 |
| `speakers` | computed | 从 `finalSegments` 提取的说话人列表（名称 + 发言条数） |
| `meetingStatus` | `ref('')` | 来自 `recording_status` 事件的会议状态 |
| `error` | `ref('')` | 错误信息 |

**方法：**

| 方法 | 说明 |
|------|------|
| `connect(meetingId)` | 连接 SSE，注册 subtitle/recording_status/heartbeat 三个事件处理器 |
| `disconnect()` | 关闭 EventSource 连接 |

**事件处理：**

| SSE 事件 | 处理逻辑 |
|---------|---------|
| `subtitle` | `isFinal=true` → 按 `segmentId` 去重存入 `finalSegments`；`false` → 更新 `interimText` |
| `recording_status` | 解析状态值写入 `meetingStatus` |
| `heartbeat` | 连接确认，不展示 |

---

### `useMinutes.js` — 会议纪要

| 方法 | 后端接口 | 用途 |
|------|---------|------|
| `generateMinutes(meetingId, regenerate)` | `POST /meetings/{id}/minutes/generate` | AI 生成纪要（支持重新生成） |
| `getMinutes(meetingId)` | `GET /meetings/{id}/minutes` | 查询会议最新纪要 |
| `getMinutesById(id)` | `GET /minutes/{id}` | 按 ID 查纪要 |
| `updateMinutes(id, data)` | `PUT /minutes/{id}` | 人工修订纪要 |
| `generateTasks(minutesId)` | `POST /minutes/{id}/tasks/generate` | 从行动项快照生成任务 |
| `deleteMinutes(id)` | `DELETE /minutes/{id}` | 删除纪要 |

调用后自动将结果写入 `store.meetingMinutes`。

---

### `useTask.js` — 任务管理

| 方法 | 后端接口 | 用途 |
|------|---------|------|
| `listTasks(params)` | `GET /tasks` | 分页查询任务 |
| `getTask(id)` | `GET /tasks/{id}` | 查询任务详情 |
| `createTask(data)` | `POST /tasks` | 手动创建任务 |
| `updateTask(id, data)` | `PUT /tasks/{id}` | 更新任务内容 |
| `updateTaskStatus(id, status)` | `PATCH /tasks/{id}/status` | 更新任务状态（TODO/DOING/DONE/CANCELLED） |
| `deleteTask(id)` | `DELETE /tasks/{id}` | 删除任务 |

**枚举映射工具（前端展示用）：**

| 方法 | 说明 |
|------|------|
| `formatPriority(p)` | `HIGH` → `高`，`MEDIUM` → `中`，`LOW` → `低` |
| `formatStatus(s)` | `TODO` → `待处理`，`DOING` → `进行中`，`DONE` → `已完成`，`CANCELLED` → `已取消` |
| `priorityClass(p)` / `statusClass(s)` | 返回对应 CSS 标签类名 |

---

## `src/views/` — 页面组件

### `Login.vue` — 登录页

**全屏独立页面**（不嵌入侧边栏布局），蓝白渐变背景 + 磨砂玻璃卡片。

**功能：** 输入用户名 → 存 `store.setUsername()` + `localStorage` → 跳转 `/transcription`。用户名仅用于前端展示，后端不鉴权，不要求必填。

**设计：** 背景 `linear-gradient(135deg, #E8F4FD → #D6E8FA)`，4 个浮动光晕圆形 blur(80px)，卡片 `backdrop-filter: blur(24px)` + 白色文字输入框 + 蓝色渐变按钮。

---

### `Dashboard.vue` — 工作台

**数据来源：** `GET /api/v1/meetings`（onMounted 后台静默加载）。

**布局：**

```
┌──────────────┬──────────────┬──────────────┬──────────────┐
│ 📊 总会议数   │ 🔴 录制中    │ ✅ 已结束    │ 📝 已生成纪要 │
└──────────────┴──────────────┴──────────────┴──────────────┘

┌─────────────────────┬─────────────────────┐
│ 📋 最近会议列表      │ 🚀 快捷操作 ×4       │
│ 点击进入会议室       │ 🎯 当前会议卡片       │
└─────────────────────┴─────────────────────┘
```

**要点：** 统计数字从 `store.stats` computed 计算，会议列表点击 `selectMeeting(m)` 将会议存入 store 并跳转会议室页。

---

### `TranscriptionView.vue` — 会议室（核心页面）

**三段式流程：**

| 会议状态 | 界面显示 | 操作 |
|---------|---------|------|
| **无会议** | 创建会议表单（标题/地点/组织者/人数/备注） | 点"创建会议" → `POST /meetings` |
| `NOT_STARTED` | 会议信息头 + "▶ 开始会议"、"✕ 取消会议"按钮 | 点"开始会议" → `POST /meetings/{id}/start` + SSE `connect()` |
| `RECORDING` | 实时字幕列表（SSE 接收） + 说话人侧边栏 + "⏹ 结束会议" | 点"结束会议" → `POST /meetings/{id}/finish` + SSE `disconnect()` |
| `FINISHED` | 成功页 + 入口按钮跳转到摘要/任务/导出 | 三个入口按钮 |
| `CANCELLED` | 已取消提示 | 可点"新建会议" |

**字幕展示逻辑：**

- `interimText` ≠ '' 时显示"正在说"条目（带闪烁光标 `|`）
- `finalSegments` 按加入顺序显示，每个条目显示说话人（颜色标签）+ 时间戳（`startMs` 转 mm:ss）+ 文本
- 说话人侧边栏从字幕统计，显示每人头像 + 姓名 + 发言条数
- 自动滚动到顶部（新内容在上面）

**会议头部的状态徽章：**
| 状态 | 样式 |
|------|------|
| NOT_STARTED | 🟡 tag-warning |
| RECORDING | 🔴 tag-danger |
| FINISHED | ✅ tag-success |
| CANCELLED | ⚪ tag-outline |

---

### `SummaryView.vue` — 会议摘要

**数据来源：** `GET /meetings/{id}/minutes`（onMounted 后台静默加载）。

**界面状态机：**

| 状态 | 显示 |
|------|------|
| 无会议 && 无纪要 | 空状态 → "暂未有会议纪要" + "去会议室"按钮 |
| 有会议但无纪要 | "当前会议尚未生成纪要" + "🤖 生成纪要"按钮 |
| 有纪要 | 完整页面（见下） |

**展示内容：**

```
┌──────────────────┬──────────────────┐
│ 📌 会议摘要       │ ✅ 重点内容       │
└──────────────────┴──────────────────┘
📋 会议决策（列表）     ✓ 决策内容
🎯 待办行动项          [生成任务 →] 按钮
📄 完整纪要（Markdown 原文）
```

**操作：**
- "🔄 重新生成" → `POST /meetings/{id}/minutes/generate`（regenerate=true）
- "生成任务 →" → `POST /minutes/{id}/tasks/generate`
- "📋 复制全文" → 复制 `content` 到剪贴板

---

### `TasksView.vue` — 待办任务

**数据来源：** `GET /tasks?meetingId=xxx`（onMounted 后台静默加载）。

**统计栏：** 总任务 / 待处理 / 进行中 / 已完成 / 高优先级（从 `store.tasks` 实时计算）。

**筛选栏：** 状态标签切换（全部/待处理/进行中/已完成）+ 优先级下拉筛选。

**任务卡片：**

```
☐ 微服务拆分详细设计方案 [🔴 高] [进行中]
   👤 王工  📅 2026-05-30  🏷 自动
   描述文本...
                              [待处理 ▼] [✕]
```

**操作：**
- 复选框 → `PATCH /tasks/{id}/status`（TODO ↔ DONE 切换）
- 状态下拉 → 直接更新状态
- ✕ 删除 → 确认后 `DELETE /tasks/{id}`
- "+ 新建任务"弹窗 → `POST /tasks`（会议 ID 来自 `store.currentMeeting`）

**枚举映射：**
| 后端 | 前端显示 |
|------|---------|
| `TODO` | 待处理 (tag-warning) |
| `DOING` | 进行中 (tag-info) |
| `DONE` | 已完成 (tag-success) |
| `CANCELLED` | 已取消 (tag-outline) |
| `HIGH` | 高 (priority-high, tag-danger) |
| `MEDIUM` | 中 (priority-medium, tag-warning) |
| `LOW` | 低 (priority-low, tag-success) |

---

### `ExportView.vue` — 导出与飞书同步

**数据来源：** `POST /minutes/{id}/exports` 触发后端生成文件。

**布局：**

```
┌────────────────────────────┬──────────────────┐
│ 📄 导出格式                 │ 🌐 飞书同步       │
│  [📝 Word]  [📕 PDF]       │                  │
│                             │ 🔗 飞书 OAuth    │
│ ⚙️ 导出选项                 │   授权           │
│  ☑ 包含任务列表             │                  │
│  ☐ 包含字幕原文             │ ☑ 同步在线文档    │
│                             │ ☐ 上传 PDF       │
│ [📤 导出]  文件已生成 · 下载 │ ☑ 同步任务       │
│                             │ [📤 同步到飞书]   │
└────────────────────────────┴──────────────────┘
```

**后端导出的优势（对比前端生成）：** Apache POI 生真正 .docx，iText 生真正 .pdf，文件存 24 小时，支持飞书在线文档导入。

**飞书 OAuth 流程：**
1. 调用 `GET /api/v1/feishu/oauth/authorize-url` → 拿到授权 URL
2. 跳转飞书授权 → 用户同意 → 回调 URL 带 `code`
3. `POST /feishu/oauth/token` → 换 token → 存 store
4. 后续同步用 USER 模式的 `oauthUserKey`

---

## 已删除的模块（v6 迁移移除）

这些文件曾存在于早期版本（v2-v5），在接入后端 API 后被删除，职责由边缘设备 + 后端接管。

| 文件 | 原职责 | 替代方案 |
|------|--------|---------|
| `useSpeechRecognition.js` | 浏览器 ASR（SpeechRecognition API） | 边缘设备 ASR |
| `useBackendAsr.js` | WebSocket 发送 WebM/Opus 到后端 | 边缘设备直连后端 |
| `useWebRTC.js` | RTCPeerConnection P2P 音频分发 | 边缘设备处理分发 |
| `useFileExport.js` | 前端 HTML/Markdown/TXT 生成与下载 | 后端 Apache POI / iText |
| `usePresence.js` | BroadcastChannel 跨标签页参会人同步 | 暂不实现 |

---

## 架构数据流

```
┌──────────┐    REST/SSE    ┌──────────┐    WebSocket    ┌──────────────┐
│  前端     │ ←───────────→ │  后端     │ ←────────────→ │  边缘设备     │
│ (浏览器)  │               │ (Spring)  │               │ (本地服务)    │
│           │               │           │               │              │
│ 创建会议   │  POST /meetings         │               │ 控制录制     │
│ 开始会议   │  POST /meetings/{id}/start → → → → → 触发开始录制     │
│ 收字幕     │  SSE ← 逐条推送 ← ← ← ← ← 字幕回传      │ ASR 识别     │
│ 结束会议   │  POST /meetings/{id}/finish → → → → → 停止录制       │
│ 查纪要     │  GET /minutes           │               │              │
│ 导出       │  POST /exports          │               │              │
└──────────┘  └──────────┘              └──────────────┘

                              │
                         飞书 API
                              ↓
                     ┌──────────────┐
                     │  飞书开放平台 │
                     │ 在线文档/任务 │
                     │  OAuth/通知  │
                     └──────────────┘
```

---

## 关键设计决策

1. **API 超时 3 秒**：避免无后端时页面挂起，快速降级显示空状态。
2. **onMounted 不阻塞**：API 请求全部 `.catch(() => {})` 后台执行，组件立即渲染。
3. **Store 只做缓存**：不存 mock 数据，数据由 composable 从 API 获取后写入。
4. **SSE 字幕去重**：按 `segmentId` 做幂等，防止重复渲染。
5. **枚举映射集中管理**：`useTask.js` 统一处理后端枚举 → 前端展示的转换。
6. **App.vue 条件渲染登录页**：登录页全屏独立，不嵌入侧边栏布局。
